# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41  nice_view_adapter nice_view
#snippet: studio-rpc-usb-uart 
---
include:
  - board: nice_nano_v2
    shield: keyball44_left nice_view_adapter nice_view
  - board: nice_nano_v2
    shield: keyball44_right nice_view_adapter nice_view
    # TEMP DIAGNOSTIC: zmk-usb-logging snippet provides the USB CDC-ACM console
    # node + logging. Restore `studio-rpc-usb-uart` after debugging.
    snippet: zmk-usb-logging
  - board: nice_nano_v2
    shield: settings_reset
