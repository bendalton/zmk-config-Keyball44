CONFIG_SPI=y
CONFIG_INPUT=y
CONFIG_ZMK_MOUSE=y
CONFIG_ZMK_EXT_POWER=y

CONFIG_PMW3610=y
CONFIG_PMW3610_CPI=1872
CONFIG_PMW3610_CPI_DIVIDOR=4
CONFIG_PMW3610_ORIENTATION_180=y
CONFIG_PMW3610_SNIPE_CPI=480
CONFIG_PMW3610_SNIPE_CPI_DIVIDOR=4
CONFIG_PMW3610_SCROLL_TICK=32
CONFIG_PMW3610_INVERT_SCROLL_X=y
CONFIG_PMW3610_INVERT_SCROLL_Y=n
CONFIG_PMW3610_SMART_ALGORITHM=y
CONFIG_PMW3610_RUN_DOWNSHIFT_TIME_MS=3264
CONFIG_PMW3610_REST1_SAMPLE_TIME_MS=20
#CONFIG_PMW3610_REST1_DOWNSHIFT_TIME_MS=9600
#CONFIG_PMW3610_REST2_SAMPLE_TIME_MS=
#CONFIG_PMW3610_REST2_DOWNSHIFT_TIME_MS=
#CONFIG_PMW3610_REST3_SAMPLE_TIME_MS=
#CONFIG_PMW3610_REST3_DOWNSHIFT_TIME_MS=
# DIAGNOSTIC (2026-07): keep the sensor out of rest modes entirely so the
# first nudge after idle isn't sampled at 50Hz. Battery-hungry -- after the
# feel test, revert to =n and tune REST1_SAMPLE_TIME_MS instead.
CONFIG_PMW3610_FORCE_AWAKE=y
# Motion-IRQ driven (not the continuous software poll). The _SW mode floods
# SPIM1 nonstop and corrupts the nice!view's SPIM0 frames -> constant glitch.
# Non-SW reads only when the ball moves (via irq P1.11), so the bus is quiet
# while typing and the display stays clean. Revert to _SW if the ball misfeeds.
# DIAGNOSTIC (2026-07): 250Hz (the driver default) instead of 125 -- still
# IRQ-driven, halves sensor latency. Watch the nice!view for regressions.
CONFIG_PMW3610_POLLING_RATE_250=y

CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n
CONFIG_PMW3610_AUTOMOUSE_TIMEOUT_MS=700

# DIAGNOSTIC (2026-07): threshold 4 was added to stop micro-jitter from
# thrashing the display thread, but the real fix turned out to be the display
# stack size below -- and the threshold acts as a deadzone that stair-steps
# slow precise movement. Try 0 (full fidelity); if the display glitch returns
# or the cursor swims at rest, step up to 1-2 before going back to 4.
CONFIG_PMW3610_MOVEMENT_THRESHOLD=0

# Fix the nice!view freeze-then-speckle on this (central) half: the ZMK
# dedicated display thread overflows its default 2048B stack -- heavy
# Montserrat-26 font + the fuller central widget set + trackball load -- then
# silently hangs, and the Sharp LCD decays (nice!view has no VCOM upkeep on
# Zephyr 3.5). Give the thread plenty of headroom.
CONFIG_ZMK_DISPLAY_DEDICATED_THREAD_STACK_SIZE=8192
